angular.module('AppControllers', [])
    .controller('IndexCtrl', ['$scope', '$location', '$localStorage', '$sessionStorage', '$rootScope', 'RestClient', '$window',
        function($scope, $location, $localStorage, $sessionStorage, $rootScope, RestClient, $window) {


        }
    ]);
